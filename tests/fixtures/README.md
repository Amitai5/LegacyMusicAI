# Test fixtures

Only synthetic, public-domain, or explicitly licensed fixtures may be committed here. Record the origin and license next to every fixture. Real artist recordings and generated identity-clone samples are prohibited.

The current tests generate tones, F0 contours, fake model outputs, and authorization
manifests in temporary directories; no real artist profile or GPU checkpoint is needed.
Keep test lyrics and identifiers synthetic and never copy private run artifacts into fixtures.
