# ACE-Step environment

Use an isolated checkout of `ACE-Step/ACE-Step-1.5` and follow its reviewed upstream installation instructions. Record the installed Git revision and model identifiers in the environment manifest before the engine is marked ready.
